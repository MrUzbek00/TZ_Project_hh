# 🧩 Система управления пользователями с кастомной авторизацией и RBAC

## Краткое описание / Short description

Данный проект — простое Django-приложение для управления пользователями с полностью кастомной системой аутентификации и авторизации. В проекте не используется стандартный Django auth и механизмы сессий — вся логика входа, сессий и прав доступа реализована вручную.

This project is a small Django app that demonstrates a custom authentication + RBAC system. Django built-in auth and sessions are not used; login, session handling and access control are implemented manually.

---

## Оглавление / Table of Contents

- [Основной функционал / Features](#основной-функционал--features)
- [Структура проекта / Project structure](#структура-проекта--project-structure)
- [Аутентификация / Authentication](#аутентификация--authentication)
- [Авторизация / RBAC (Role-Based Access Control)](#авторизация--rbac-role-based-access-control)
- [Главная страница / Home page behavior](#главная-страница--home-page-behavior)
- [Обновление и удаление пользователя / Update & Delete user](#обновление-и-удаление-пользователя--update--delete-user)
- [Тестовые данные / Test data](#тестовые-данные--test-data)
- [Как запускать / How to run (dev)](#как-запускать--how-to-run-dev)
- [Проверка и отладка / Troubleshooting & checks]

---

## Основной функционал / Features

- Регистрация пользователей (signup)
- Вход/выход (login / logout)
- Кастомные сессии (таблица `Session`, UUID в cookie)
- Роли пользователей (`admin` / `manager` / `user`)
- RBAC со хранением правил в БД (функция `can` в `permissions.py`)
- Разделение прав на `own` / `all` (например, `read_permission` vs `read_all_permission`)
- Soft delete (пометка `is_active=False`)
- Инлайн-редактирование данных на странице `home`

This app demonstrates:
- custom session-based authentication
- RBAC stored in the database
- own vs all permissions
- simple user management UI

---

## Структура проекта / Project structure

Tz_Project/
- Tz_App/
  - `models.py` — модели: `UserProfile`, `Roles`, `Session`, `BusinessElement`, `AccessRoleRule`
  - `views.py` — обработчики: `signup`, `login`, `logout`, `home`, `update_user`, `delete_user`
  - `permissions.py` — RBAC-логика (`can`)
  - `middleware.py` — (рекомендуется) middleware для привязки cookie -> `request.custom_user`
  - `templates/Tz_App/` — `login.html`, `signup.html`, `home.html`
  - `migrations/` — миграции
- `manage.py`

---

## Аутентификация / Authentication

Как это работает (русский):
1. Пользователь вводит email и пароль.
2. Пароль проверяется с помощью `check_password`.
3. При успешной проверке создаётся запись в таблице `Session` с UUID.
4. UUID сессии отправляется клиенту в `HttpOnly` cookie.
5. Для последующих запросов middleware читает cookie, валидирует `Session` и устанавливает `request.custom_user`.

Notes (English): the app does not use Django `request.user`; instead `request.custom_user` must be set by middleware that reads the session cookie.

**Важно / Important:**
- Cookie name в проекте по умолчанию: `custom_sessionid` — middleware должен читать именно его.
- Пароли хранятся как хэши (`make_password`) — поле `password` в модели должно иметь достаточно большой `max_length` (рекомендуется >= 128) чтобы избежать усечения хэша.

---

## Авторизация / RBAC (Role-Based Access Control)

Центральная точка — функция:

```python
can(user, element, action, owner_id=None)
```

Поддерживаемые действия: `read`, `create`, `update`, `delete`.

В базе хранятся булевые флаги вида:
- `*_permission` — разрешение для собственных объектов (own)
- `*_all_permission` — разрешение для всех объектов (all)

Пример: `can(user, "users", "update", owner_id=2)` — можно ли пользователю обновить профиль с id=2.

Роли и их ожидания:
- `admin` — полный доступ
- `manager` — просмотр многих, изменение/удаление только своих (в зависимости от правил)
- `user` — доступ только к своим данным

---

## Главная страница / Home page behavior

Логика:
- Сначала вызывается `require_login`, который проверяет `request.custom_user` (устанавливается middleware).
- Через `can()` проверяется, разрешено ли читать всех (`read_all_permission`). Если да — отображается список пользователей (возможно с фильтрацией для менеджеров).
- Если `read_all` не разрешено, проверяем `read` для собственного профиля — тогда показываем только текущего пользователя.
- В противном случае возвращается `403` с причиной.

---

## Обновление / Удаление пользователя

Update (`update_user`):
- Только POST-запросы.
- Проверка прав через `can(cu, "users", "update", owner_id=...)`.
- Пароль перезаписывается только если введено новое значение (хранится как хэш).

Delete (`delete_user`):
- Только POST.
- Soft delete: `is_active=False`.
- Деактивация всех связанных сессий (`Session.objects.filter(user=...).update(is_active=False)`).

---

## Тестовые данные / Sample users

Создание данных через Django shell:

```bash
python manage.py shell
```

```python
from Tz_App.models import Roles, UserProfile
from django.contrib.auth.hashers import make_password

Roles.objects.all().delete()
r_admin = Roles.objects.create(role="admin")
r_manager = Roles.objects.create(role="manager")
r_user = Roles.objects.create(role="user")

UserProfile.objects.create(
    name="Admin", last_name="One", paternity_name="", email="admin@test.com",
    password=make_password("admin123"), role=r_admin, is_active=True
)
UserProfile.objects.create(
    name="Manager", last_name="Two", paternity_name="", email="manager@test.com",
    password=make_password("manager123"), role=r_manager, is_active=True
)
UserProfile.objects.create(
    name="User", last_name="Three", paternity_name="", email="user@test.com",
    password=make_password("user123"), role=r_user, is_active=True
)
```

Credentials / Учётные данные:

| Role / Роль | Email | Password |
|-------------|-------|----------|
| Admin       | admin@mail.com | 1234567 |
| Manager     | ben@example.ru | 1234567 |
| User        | tillo@mail.com | 1234567 |

---

## Как запускать (разработка) / How to run (dev)

1. Создайте виртуальное окружение и установите зависимости (например, Django).
2. Запустите сервер разработки:

```bash
python manage.py runserver
```

3. Создайте тестовые роли и пользователей через shell (см. раздел выше) или используйте UI регистрации.

---

## Тестирование / Testing

1. Войти под разными ролями (используйте указанные креденшалы).
2. Перейти на `/home/`.
3. Проверить:
   - корректность отображения списка пользователей;
   - возможность редактирования и удаления в соответствии с ролью;
   - что кнопки/опции появляются или скрываются в зависимости от прав;
   - что при попытке выполнить запрещённое действие возвращается `403`.

Для curl (пример входа):

```bash
curl -i -X POST -d "email=user@test.com&password=user123" http://127.0.0.1:8000/login/
# затем использовать cookie из заголовка Set-Cookie для следующих запросов
```

---

## Частые проблемы и отладка / Troubleshooting & checks

- Middleware отсутствует или не зарегистрирован — cookie ставится, но `request.custom_user` не устанавливается → `require_login` возвращает 401. Решение: создать и зарегистрировать `Tz_App.middleware.SessionAuthMiddleware`.
- Несовпадение имени cookie — убедитесь, что middleware читает то же имя, которое выставляет `views.login` (`custom_sessionid`).
- Хэш пароля усечён в БД — убедитесь в достаточной длине поля `password` (рекомендуется `max_length >= 128`).
- Таймаут сессии — проверьте `expires_at` и используйте `django.utils.timezone.now()` последовательно.

---

